---
import Icon from 'astro-icon';

interface Props {
  name: string;
}

const { name } = Astro.props;
---
<div class="w-14 h-14 rounded-full bg-accent-bg p-3">
  <Icon name={name} class="text-white" size="32" />
</div>